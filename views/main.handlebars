<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Tech Blog</title>


</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/"><button class="btn-no-style"> Tech Blog</button></a>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a href="/"><button class="btn-no-style">homepage</button></a>
                <a href="/dashboard"><button class="btn-no-style">dashboard</button></a>
                <a href="/dashboard/create/"><button class="btn-no-style">create post</button></a>
                <button id="logout" class="btn-no-style">logout</button>
                {{else}}
                <a href="/"><button class="btn-no-style">homepage</button></a>
                <a href="/login"><button class="btn-no-style">login</button></a>
                {{/if}}
            </nav>
        </header>

        <main>
            {{{ body }}}
        </main>

        <footer>
        {{#if loggedIn}}
        <div class="text-centered">  
            <a href="/"><button class="btn-no-style">homepage</button></a>
            <a href="/dashboard"><button class="btn-no-style">dashboard</button></a>
            <a href="/dashboard/create/"><button class="btn-no-style">create post</button></a>
            <button id="logout" class="btn-no-style">logout</button>
            {{else}}
            <a href="/"><button class="btn-no-style">homepage</button></a>
            <a href="/login"><button class="btn-no-style">login</button></a>
        </div>
        {{/if}} 
        </footer>
    </div>
    
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>